# Fixa stubs med rätt arguments för RuleHandler.new(sdsd).rules
# Fix cucumber exit tests
# Run cucumber after tests if possible
# Fix some tree structure for S-exp
# Change up to something similar to rspec fire?
